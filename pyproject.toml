[project]
name = "pixiechess-analytics"
version = "0.1.0"
description = "Interactive web dashboard for PixieChess analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "marimo>=0.22.4",
]
